# Installation

1. Install Node.js 20 or newer.
2. Run `npm install`.
3. Run `npm run dev`.
4. Open the Vite URL shown in the terminal.
5. For production, run `npm run build`.
6. Push the repository to GitHub and enable GitHub Pages -> GitHub Actions.

The repository intentionally does not include node_modules.
